<p>
  <%= link_to 'Zurück zur Taskboardübersicht', taskboard_collections_path %>

</p>


<h1><%= TaskboardCollection.find(session[:taskboard]).name %> </h1> <%= link_to 'Neu Task anlegen', new_task_path %>



<table class="table">
  <thead>
  <tr>
    <th>InBox</th>
    <th>InProcess</th>
    <th>InReview</th>
    <th>Complete</th>
  </tr>
  <tr>
    <td>
      <% @tasks.where(category_id: 1).each do |task| %>
          <div>
            <div class="panel panel-info">
              <div class="panel-heading">
                <table width="100%"><tr>
                  <td align="left">
                <h3 class="panel-title"><%= task.title %></h3>
                  </td>
                  <td>&nbsp;</td>
                  <td align="right">
                  <ul class="nav navbar-nav navbar-collapse collapse" style="float:right;">
                    <li class="dropdown">
                      <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="true"><span class="glyphicon glyphicon-cog" aria-hidden="true"></span> <span class="caret"></span></a>
                      <ul class="dropdown-menu" role="menu">
                        <li><%= link_to 'anzeigen', task %></li>
                        <li><%= link_to 'bearbeiten', edit_task_path(task) %></li>
                        <li><%= link_to 'löschen', task, method: :delete , data: { confirm: 'Are you sure?' }%></li>
                      </ul>
                    </li>
                  </ul>
                  </td>
                </tr></table>
              </div>
              <div class="panel-body">
                <%= task.aufgabe %>
              </div>
            </div>
          </div>
          <br>
      <% end %>

    </td>
    <td>
      <% @tasks.where(category_id: 2).each do |task| %>
          <div>
            <div class="panel panel-danger">
              <div class="panel-heading">
                <table width="100%"><tr>
                  <td align="left">
                    <h3 class="panel-title"><%= task.title %></h3>
                  </td>
                  <td>&nbsp;</td>
                  <td align="right">
                    <ul class="nav navbar-nav navbar-collapse collapse" style="float:right;">
                      <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="true"><span class="glyphicon glyphicon-cog" aria-hidden="true"></span> <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                          <li><%= link_to 'anzeigen', task %></li>
                          <li><%= link_to 'bearbeiten', edit_task_path(task) %></li>
                          <li><%= link_to 'löschen', task, method: :delete , data: { confirm: 'Are you sure?' }%></li>
                        </ul>
                      </li>
                    </ul>
                  </td>
                </tr></table>
              </div>
              <div class="panel-body">
                <%= task.aufgabe %>
              </div>
            </div>
            </div>
              <br>
      <% end %>
    </td>
    <td>
      <% @tasks.where(category_id: 3).each do |task| %>
          <div>
            <div class="panel panel-warning">
              <div class="panel-heading">
                <table width="100%"><tr>
                  <td align="left">
                    <h3 class="panel-title"><%= task.title %></h3>
                  </td>
                  <td>&nbsp;</td>
                  <td align="right">
                    <ul class="nav navbar-nav navbar-collapse collapse" style="float:right;">
                      <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="true"><span class="glyphicon glyphicon-cog" aria-hidden="true"></span> <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                          <li><%= link_to 'anzeigen', task %></li>
                          <li><%= link_to 'bearbeiten', edit_task_path(task) %></li>
                          <li><%= link_to 'löschen', task, method: :delete , data: { confirm: 'Are you sure?' }%></li>
                        </ul>
                      </li>
                    </ul>
                  </td>
                </tr></table>
              </div>
              <div class="panel-body">
                <%= task.aufgabe %>
              </div>
            </div>
          </div>
              <br>
      <% end %>
   </td><td>
      <% @tasks.where(category_id: 4).each do |task| %>
        <div>
          <div class="panel panel-success">
            <div class="panel-heading">
              <table width="100%"><tr>
                <td align="left">
                  <h3 class="panel-title"><%= task.title %></h3>
                </td>
                <td>&nbsp;</td>
                <td align="right">
                  <ul class="nav navbar-nav navbar-collapse collapse" style="float:right;">
                    <li class="dropdown">
                      <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="true"><span class="glyphicon glyphicon-cog" aria-hidden="true"></span> <span class="caret"></span></a>
                      <ul class="dropdown-menu" role="menu">
                        <li><%= link_to 'anzeigen', task %></li>
                        <li><%= link_to 'bearbeiten', edit_task_path(task) %></li>
                        <li><%= link_to 'löschen', task, method: :delete , data: { confirm: 'Are you sure?' }%></li>
                      </ul>
                    </li>
                  </ul>
                </td>
              </tr></table>
            </div>
            <div class="panel-body">
              <%= task.aufgabe %>
            </div>
          </div>
        </div>
            <br>
      <% end %>
    </td>
  </tr>
  </thead>
</table>


<br>




